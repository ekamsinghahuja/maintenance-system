export type OnboardingState = {
  error?: string;
};

export type InitiativeActionState = {
  error?: string;
  success?: string;
};

export type PaymentActionState = {
  error?: string;
  success?: string;
};

